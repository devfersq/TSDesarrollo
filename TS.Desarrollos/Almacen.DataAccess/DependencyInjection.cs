namespace Almacen.DataAccess
{
    public static class DependencyInjection
    {
        //public static void AddPersistence(this IServiceCollection services, IConfiguration config)
        //{
        //    services.AddDbContext<DataAccessContext>(opt => opt.UseSqlServer(config.GetConnectionString("InterfacesUIContext")));
        //    services.AddScoped<IDataAccessContext>(provider => provider.GetService<DataAccessContext>());
        //}
    }
}
